# 更新日志

## 2026-05-19

- README：新增欢迎提交 Issue / Pull Request 的说明，并补充项目已在 LINUX DO 社区发布。
- OSS PR Agent：新增 `/method <主题|all>` 斜杠指令，用于动态切换寻找 issue 的主题；`all` 会恢复默认方向。
- OSS PR Agent：新增 CPA budget 查询缓存与节流。成功结果缓存 10 分钟，失败结果缓存 30 分钟，最小查询间隔 10 分钟。
- OSS PR Agent：减少 CPA budget 查询的无效 fallback；没有 `auth_index` 的 Codex auth 文件只按文件名查询一次，避免每个账号额外打一发必失败请求。
