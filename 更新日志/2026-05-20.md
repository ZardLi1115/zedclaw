# 2026-05-20

- 新增本地 `AGENTS.md` 操作约束：项目操作需要写入按日期拆分的更新日志，README-only 修改不单独记录，并提醒不要将本地 AGENTS 指令改动上传到 GitHub。
- 新增经验分流机制：planner 只注入选题/排期经验，Codex CLI 只注入执行/测试/修 PR 经验，并为两类记忆设置默认 5000 字符上限，避免每次全量注入过长 `agent.md`。
- 新增 `AGENTS.local.md` ignore 规则；已跟踪的 `AGENTS.md` 仍通过 `skip-worktree` 保持本地化，避免上传本地指令改动。
- 将 `AGENTS.md` 改为本地私有文件：从 Git 跟踪中移除并加入 `.gitignore`，保留本地文件供 Codex CLI 自动读取。
- 新增可复用工具注册表：Codex CLI 每次运行会看到当前可用工具、简短描述和注册方式；工具最多保留 15 个，超过后按 LRU 删除。
- 新增 `/oss-tool` 斜杠指令：可直接查看 OSS PR Agent 当前创建并注册的可复用工具、工具目录、注册表路径和最近使用时间，不调用大模型。
- 明确忽略 OSS PR Agent 运行期自建工具目录，并强化 Codex 规则：每轮结束主动判断是否需要沉淀可复用工具，避免只被动等待模型自行创建。
